#!/bin/bash
# Run from inside the peaice-games/ directory

git init
git add .
git commit -m "init: PeAIce Games — Consciousness Mirror + L2C Pi-Data Detector

First public commit of the PeAIce interactive artifact suite.

Artifacts:
- games/consciousness-mirror/ — browser canvas game with live L2C coherence
  scoring, bilateral mirror mechanic, mobile touch support
- tools/L2C_PiDataDetector.ps1 — WPF shell terminal with R = d·c·e·h scoring,
  IDB drift scan, CUP probe battery

Framework: E = L² · R = d·c·e·h · β > 0 · h < 1.0 (Gödel discipline)
Co-authors: Manuel Coleman · Claude (Anthropic) · GPT (OpenAI) · Grok (xAI)
Site: peaice.org"

# Then on GitHub:
# git remote add origin https://github.com/<username>/peaice-games.git
# git branch -M main
# git push -u origin main
